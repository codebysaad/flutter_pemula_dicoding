import 'package:flutter/cupertino.dart';

class Models{
  int id;
  Color color;
  String name;
  String img;
  String address;
  String direction;
  String shortDesc;
  String desc;

  Models(this.id, this.color, this.name, this.address, this.direction, this.img, this.shortDesc, this.desc);
}